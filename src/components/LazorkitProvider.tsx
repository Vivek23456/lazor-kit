"use client";

/**
 * Lazorkit Provider - Re-exports from official SDK
 * 
 * This file provides the LazorkitProvider and useWallet hook from the
 * official Lazorkit SDK (@lazorkit/monorepo).
 */

// Re-export the official SDK's LazorkitProvider
export { LazorkitProvider } from '@lazorkit/monorepo/packages/ts-sdk';

// Re-export the official SDK's useWallet hook
export { useWallet } from '@lazorkit/monorepo/packages/ts-sdk';
